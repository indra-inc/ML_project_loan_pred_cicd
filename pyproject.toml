[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "loan-pred"
version = "0.1.0"
description = "MLflow based loan approval prediction project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]